/**
 * CityPay Payment API
 *  Welcome to the CityPay API, a robust HTTP API payment solution designed for seamless server-to-server  transactional processing. Our API facilitates a wide array of payment operations, catering to diverse business needs.  Whether you're integrating Internet payments, handling Mail Order/Telephone Order (MOTO) transactions, managing  Subscriptions with Recurring and Continuous Authority payments, or navigating the complexities of 3-D Secure  authentication, our API is equipped to support your requirements. Additionally, we offer functionalities for  Authorisation, Refunding, Pre-Authorisation, Cancellation/Voids, and Completion processing, alongside the capability  for tokenised payments.  ## Compliance and Security Overview <aside class=\"notice\">   Ensuring the security of payment transactions and compliance with industry standards is paramount. Our API is    designed with stringent security measures and compliance protocols to safeguard sensitive information and meet    the rigorous requirements of Visa, MasterCard, and the PCI Security Standards Council. </aside>  ### Key Compliance and Security Measures  * **TLS Encryption**: All data transmissions must utilise TLS version 1.2 or higher, employing [strong cryptography](#enabled-tls-ciphers). Our infrastructure strictly enforces this requirement to maintain the integrity and confidentiality of data in transit. We conduct regular scans and assessments of our TLS endpoints to identify and mitigate vulnerabilities. * **Data Storage Prohibitions**: Storing sensitive cardholder data (CHD), such as the card security code (CSC) or primary account number (PAN), is strictly prohibited. Our API is designed to minimize your exposure to sensitive data, thereby reducing your compliance burden. * **Data Masking**: For consumer protection and compliance, full card numbers must not be displayed on receipts or any customer-facing materials. Our API automatically masks PANs, displaying only the last four digits to facilitate safe receipt generation. * **Network Scans**: If your application is web-based, regular scans of your hosting environment are mandatory to identify and rectify potential vulnerabilities. This proactive measure is crucial for maintaining a secure and compliant online presence. * **PCI Compliance**: Adherence to PCI DSS standards is not optional; it's a requirement for operating securely and legally in the payments ecosystem. For detailed information on compliance requirements and resources, please visit the PCI Security Standards Council website [https://www.pcisecuritystandards.org/](https://www.pcisecuritystandards.org/). * **Request Validation**: Our API includes mechanisms to verify the legitimacy of each request, ensuring it pertains to a valid account and originates from a trusted source. We leverage remote IP address verification alongside sophisticated application firewall technologies to thwart a wide array of common security threats.  ## Getting Started Before integrating with the CityPay API, ensure your application and development practices align with the outlined compliance and security measures. This preparatory step is crucial for a smooth integration process and the long-term success of your payment processing operations.  For further details on API endpoints, request/response formats, and code examples, proceed to the subsequent sections of our documentation. Our aim is to provide you with all the necessary tools and information to integrate our payment processing capabilities seamlessly into your application.  Thank you for choosing CityPay API. We look forward to supporting your payment processing needs with our secure, compliant, and versatile API solution. 
 *
 * Contact: support@citypay.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Adjustments from './Adjustments';
import ContactDetails from './ContactDetails';

/**
 * The PaymentIntentRequestModel model module.
 * @module model/PaymentIntentRequestModel
 */
class PaymentIntentRequestModel {
    /**
     * Constructs a new <code>PaymentIntentRequestModel</code>.
     * @alias module:model/PaymentIntentRequestModel
     * @param amount {Number} The amount to authorise in the lowest unit of currency with a variable length to a maximum of 12 digits.  No decimal points are to be included and no divisional characters such as 1,024.  The amount should be the total amount required for the transaction.  For example with GBP £1,021.95 the amount value is 102195. 
     * @param identifier {String} The identifier of the transaction to process. The value should be a valid reference and may be used to perform  post processing actions and to aid in reconciliation of transactions.  The value should be a valid printable string with ASCII character ranges from 0x32 to 0x127.  The identifier is recommended to be distinct for each transaction such as a [random unique identifier](https://en.wikipedia.org/wiki/Universally_unique_identifier) this will aid in ensuring each transaction is identifiable.  When transactions are processed they are also checked for duplicate requests. Changing the identifier on a subsequent request will ensure that a transaction is considered as different. 
     */
    constructor(amount, identifier) { 
        
        PaymentIntentRequestModel.initialize(this, amount, identifier);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, identifier) { 
        obj['amount'] = amount;
        obj['identifier'] = identifier;
    }

    /**
     * Constructs a <code>PaymentIntentRequestModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentIntentRequestModel} obj Optional instance to populate.
     * @return {module:model/PaymentIntentRequestModel} The populated <code>PaymentIntentRequestModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentIntentRequestModel();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('identifier')) {
                obj['identifier'] = ApiClient.convertToType(data['identifier'], 'String');
            }
            if (data.hasOwnProperty('adjustments')) {
                obj['adjustments'] = Adjustments.constructFromObject(data['adjustments']);
            }
            if (data.hasOwnProperty('avs_postcode_policy')) {
                obj['avs_postcode_policy'] = ApiClient.convertToType(data['avs_postcode_policy'], 'String');
            }
            if (data.hasOwnProperty('bill_to')) {
                obj['bill_to'] = ContactDetails.constructFromObject(data['bill_to']);
            }
            if (data.hasOwnProperty('csc_policy')) {
                obj['csc_policy'] = ApiClient.convertToType(data['csc_policy'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('duplicate_policy')) {
                obj['duplicate_policy'] = ApiClient.convertToType(data['duplicate_policy'], 'String');
            }
            if (data.hasOwnProperty('external-ref')) {
                obj['external-ref'] = ApiClient.convertToType(data['external-ref'], 'String');
            }
            if (data.hasOwnProperty('external-ref-source')) {
                obj['external-ref-source'] = ApiClient.convertToType(data['external-ref-source'], 'String');
            }
            if (data.hasOwnProperty('match_avsa')) {
                obj['match_avsa'] = ApiClient.convertToType(data['match_avsa'], 'String');
            }
            if (data.hasOwnProperty('merchantid')) {
                obj['merchantid'] = ApiClient.convertToType(data['merchantid'], 'Number');
            }
            if (data.hasOwnProperty('pre_auth')) {
                obj['pre_auth'] = ApiClient.convertToType(data['pre_auth'], 'String');
            }
            if (data.hasOwnProperty('ship_to')) {
                obj['ship_to'] = ContactDetails.constructFromObject(data['ship_to']);
            }
            if (data.hasOwnProperty('tag')) {
                obj['tag'] = ApiClient.convertToType(data['tag'], ['String']);
            }
            if (data.hasOwnProperty('trans_info')) {
                obj['trans_info'] = ApiClient.convertToType(data['trans_info'], 'String');
            }
            if (data.hasOwnProperty('trans_type')) {
                obj['trans_type'] = ApiClient.convertToType(data['trans_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentIntentRequestModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentIntentRequestModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentIntentRequestModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['identifier'] && !(typeof data['identifier'] === 'string' || data['identifier'] instanceof String)) {
            throw new Error("Expected the field `identifier` to be a primitive type in the JSON string but got " + data['identifier']);
        }
        // validate the optional field `adjustments`
        if (data['adjustments']) { // data not null
          Adjustments.validateJSON(data['adjustments']);
        }
        // ensure the json data is a string
        if (data['avs_postcode_policy'] && !(typeof data['avs_postcode_policy'] === 'string' || data['avs_postcode_policy'] instanceof String)) {
            throw new Error("Expected the field `avs_postcode_policy` to be a primitive type in the JSON string but got " + data['avs_postcode_policy']);
        }
        // validate the optional field `bill_to`
        if (data['bill_to']) { // data not null
          ContactDetails.validateJSON(data['bill_to']);
        }
        // ensure the json data is a string
        if (data['csc_policy'] && !(typeof data['csc_policy'] === 'string' || data['csc_policy'] instanceof String)) {
            throw new Error("Expected the field `csc_policy` to be a primitive type in the JSON string but got " + data['csc_policy']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['duplicate_policy'] && !(typeof data['duplicate_policy'] === 'string' || data['duplicate_policy'] instanceof String)) {
            throw new Error("Expected the field `duplicate_policy` to be a primitive type in the JSON string but got " + data['duplicate_policy']);
        }
        // ensure the json data is a string
        if (data['external-ref'] && !(typeof data['external-ref'] === 'string' || data['external-ref'] instanceof String)) {
            throw new Error("Expected the field `external-ref` to be a primitive type in the JSON string but got " + data['external-ref']);
        }
        // ensure the json data is a string
        if (data['external-ref-source'] && !(typeof data['external-ref-source'] === 'string' || data['external-ref-source'] instanceof String)) {
            throw new Error("Expected the field `external-ref-source` to be a primitive type in the JSON string but got " + data['external-ref-source']);
        }
        // ensure the json data is a string
        if (data['match_avsa'] && !(typeof data['match_avsa'] === 'string' || data['match_avsa'] instanceof String)) {
            throw new Error("Expected the field `match_avsa` to be a primitive type in the JSON string but got " + data['match_avsa']);
        }
        // ensure the json data is a string
        if (data['pre_auth'] && !(typeof data['pre_auth'] === 'string' || data['pre_auth'] instanceof String)) {
            throw new Error("Expected the field `pre_auth` to be a primitive type in the JSON string but got " + data['pre_auth']);
        }
        // validate the optional field `ship_to`
        if (data['ship_to']) { // data not null
          ContactDetails.validateJSON(data['ship_to']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tag'])) {
            throw new Error("Expected the field `tag` to be an array in the JSON data but got " + data['tag']);
        }
        // ensure the json data is a string
        if (data['trans_info'] && !(typeof data['trans_info'] === 'string' || data['trans_info'] instanceof String)) {
            throw new Error("Expected the field `trans_info` to be a primitive type in the JSON string but got " + data['trans_info']);
        }
        // ensure the json data is a string
        if (data['trans_type'] && !(typeof data['trans_type'] === 'string' || data['trans_type'] instanceof String)) {
            throw new Error("Expected the field `trans_type` to be a primitive type in the JSON string but got " + data['trans_type']);
        }

        return true;
    }


}

PaymentIntentRequestModel.RequiredProperties = ["amount", "identifier"];

/**
 * The amount to authorise in the lowest unit of currency with a variable length to a maximum of 12 digits.  No decimal points are to be included and no divisional characters such as 1,024.  The amount should be the total amount required for the transaction.  For example with GBP £1,021.95 the amount value is 102195. 
 * @member {Number} amount
 */
PaymentIntentRequestModel.prototype['amount'] = undefined;

/**
 * The identifier of the transaction to process. The value should be a valid reference and may be used to perform  post processing actions and to aid in reconciliation of transactions.  The value should be a valid printable string with ASCII character ranges from 0x32 to 0x127.  The identifier is recommended to be distinct for each transaction such as a [random unique identifier](https://en.wikipedia.org/wiki/Universally_unique_identifier) this will aid in ensuring each transaction is identifiable.  When transactions are processed they are also checked for duplicate requests. Changing the identifier on a subsequent request will ensure that a transaction is considered as different. 
 * @member {String} identifier
 */
PaymentIntentRequestModel.prototype['identifier'] = undefined;

/**
 * @member {module:model/Adjustments} adjustments
 */
PaymentIntentRequestModel.prototype['adjustments'] = undefined;

/**
 * A policy value which determines whether an AVS postcode policy is enforced or bypassed.  Values are:   `0` for the default policy (default value if not supplied). Your default values are determined by your account manager on setup of the account.   `1` for an enforced policy. Transactions that are enforced will be rejected if the AVS postcode numeric value does not match.   `2` to bypass. Transactions that are bypassed will be allowed through even if the postcode did not match.   `3` to ignore. Transactions that are ignored will bypass the result and not send postcode details for authorisation. 
 * @member {String} avs_postcode_policy
 */
PaymentIntentRequestModel.prototype['avs_postcode_policy'] = undefined;

/**
 * @member {module:model/ContactDetails} bill_to
 */
PaymentIntentRequestModel.prototype['bill_to'] = undefined;

/**
 * A policy value which determines whether a CSC policy is enforced or bypassed.  Values are:   `0` for the default policy (default value if not supplied). Your default values are determined by your account manager on setup of the account.   `1` for an enforced policy. Transactions that are enforced will be rejected if the CSC value does not match.   `2` to bypass. Transactions that are bypassed will be allowed through even if the CSC did not match.   `3` to ignore. Transactions that are ignored will bypass the result and not send the CSC details for authorisation. 
 * @member {String} csc_policy
 */
PaymentIntentRequestModel.prototype['csc_policy'] = undefined;

/**
 * The processing currency for the transaction. Will default to the merchant account currency.
 * @member {String} currency
 */
PaymentIntentRequestModel.prototype['currency'] = undefined;

/**
 * A policy value which determines whether a duplication policy is enforced or bypassed. A duplication check has a window of time set against your account within which it can action. If a previous transaction with matching values occurred within the window, any subsequent transaction will result in a T001 result.  Values are   `0` for the default policy (default value if not supplied). Your default values are determined by your account manager on setup of the account.   `1` for an enforced policy. Transactions that are enforced will be checked for duplication within the duplication window.   `2` to bypass. Transactions that are bypassed will not be checked for duplication within the duplication window.   `3` to ignore. Transactions that are ignored will have the same affect as bypass. 
 * @member {String} duplicate_policy
 */
PaymentIntentRequestModel.prototype['duplicate_policy'] = undefined;

/**
 * A unique identifier, such as an order ID or invoice number, provided by your accounting or billing system to link the payment intent with an external system reference. This ensures traceability across systems for audits and transaction validation.
 * @member {String} external-ref
 */
PaymentIntentRequestModel.prototype['external-ref'] = undefined;

/**
 * Specifies the originating source or system of the external reference, helping to categorise and trace the context of the external identifier, whether it comes from an internal system, third-party vendor, or external financial platform.
 * @member {String} external-ref-source
 */
PaymentIntentRequestModel.prototype['external-ref-source'] = undefined;

/**
 * A policy value which determines whether an AVS address policy is enforced, bypassed or ignored.  Values are:   `0` for the default policy (default value if not supplied). Your default values are determined by your account manager on setup of the account.   `1` for an enforced policy. Transactions that are enforced will be rejected if the AVS address numeric value does not match.   `2` to bypass. Transactions that are bypassed will be allowed through even if the address did not match.   `3` to ignore. Transactions that are ignored will bypass the result and not send address numeric details for authorisation. 
 * @member {String} match_avsa
 */
PaymentIntentRequestModel.prototype['match_avsa'] = undefined;

/**
 * The merchant id of the intent, required if using the API key or not required if using a domain key.
 * @member {Number} merchantid
 */
PaymentIntentRequestModel.prototype['merchantid'] = undefined;

/**
 * A policy value which determines whether a pre auth policy is enforced or bypassed.  Values are:   `0` for the default policy (default value if not supplied). Your default values are determined by your account manager on setup of the account.   `1` for an enforced policy.  Enforces pre-authorisation when it does not pre-auth by default.   `2` to bypass. Bypasses pre-authorisation when it is enabled to pre auth by default.   `3` to ignore. The same as the default policy (0). Although it currently mirrors the default, this option is included for compatibility with other policies. 
 * @member {String} pre_auth
 */
PaymentIntentRequestModel.prototype['pre_auth'] = undefined;

/**
 * @member {module:model/ContactDetails} ship_to
 */
PaymentIntentRequestModel.prototype['ship_to'] = undefined;

/**
 * @member {Array.<String>} tag
 */
PaymentIntentRequestModel.prototype['tag'] = undefined;

/**
 * Further information that can be added to the transaction will display in reporting. Can be used for flexible values such as operator id.
 * @member {String} trans_info
 */
PaymentIntentRequestModel.prototype['trans_info'] = undefined;

/**
 * The type of transaction being submitted. Normally this value is not required and your account manager may request that you set this field.
 * @member {String} trans_type
 */
PaymentIntentRequestModel.prototype['trans_type'] = undefined;






export default PaymentIntentRequestModel;

